export enum ParentType {
  POST = 'POST',
  COMMENT = 'COMMENT',
}

export enum RoleType {
  USER = 'user',
  ADMIN = 'admin',
}

export enum PublicationState {
  LIVE = 'live',
  PREVIEW = 'preview',
}